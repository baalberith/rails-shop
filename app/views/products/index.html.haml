%h2 Products

- @products.each do |product|
  %p
    Name:
    = product.name
    %br
    Price:
    = product.price
    = link_to "Add to cart", add_to_cart_product_path(product)
    
= link_to "Cart", cart_path
